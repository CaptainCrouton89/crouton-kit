- Stop hook input does not include assistant message content — `backwards-compat-warning.py` must read `transcript_path` directly to inspect what Claude said; reading from hook input silently returns nothing.
- Any Stop hook that emits `decision: block` re-triggers Stop — `viz-stop-debug.sh` guards `stop_hook_active == True` to break the loop; omitting this causes infinite recursion when another blocking Stop hook fires.
- `render-viz` in `viz-stop-debug.sh` must run fully detached (`nohup ... disown`) — a foreground exec holds the Stop hook open and blocks Claude from resuming.
