---
description: Launch a code reviewer of the working tree in a new pane; close current pane
allowed-tools: Bash(crtr:*)
---

The user wants to hand off a code review of the working tree via `crtr agent review`. Begin by following the CLI guidance below.

!`crtr agent review --help`

$ARGUMENTS
