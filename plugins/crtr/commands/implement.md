---
description: Launch an implementer for an approved plan in a new pane
allowed-tools: Bash(crtr:*)
---

The user wants to hand off implementation of an approved plan. Resolve the plan path with `crtr mode plan show <name>` (returns `.path`) if needed, then follow the CLI guidance below.

!`crtr mode implementer -h`

$ARGUMENTS
