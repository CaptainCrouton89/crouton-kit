---
description: Launch an implementer for an approved plan in a new pane; close current pane
allowed-tools: Bash(crtr:*)
---

The user wants to hand off implementation of an approved plan via `crtr agent implement`. Begin by following the CLI guidance below.

!`crtr agent implement --help`

$ARGUMENTS
