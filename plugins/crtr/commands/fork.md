---
description: Fork the current Claude Code session into a sibling pane with a new prompt
allowed-tools: Bash(crtr:*)
---

The user wants to fork the current Claude Code session into a sibling pane via `crtr agent fork`. Begin by following the CLI guidance below.

!`crtr agent fork --help`

$ARGUMENTS
