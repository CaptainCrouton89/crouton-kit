---
description: Spawn a fresh claude in a sibling pane (async; prints session id)
allowed-tools: Bash(crtr:*)
---

The user wants to spawn a fresh sibling claude pane via `crtr agent new`. Begin by following the CLI guidance below.

!`crtr agent new --help`

$ARGUMENTS
