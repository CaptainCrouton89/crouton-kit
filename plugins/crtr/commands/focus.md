---
description: Move a running agent's pane into your view (--new | --replace).
allowed-tools: Bash(crtr:*)
---

Move a running agent's pane into your view side-by-side or replacing the caller. Follow the CLI guidance below.

!`crtr agent focus -h`

$ARGUMENTS
