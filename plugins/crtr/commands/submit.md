---
description: Inside a crtr-spawned session, deliver content back to the parent (uses $CRTR_PIPE)
allowed-tools: Bash(crtr:*)
---

The user wants to submit content back to the parent crtr session via `crtr agent submit`. Begin by following the CLI guidance below.

!`crtr agent submit --help`

$ARGUMENTS
