---
description: Block until a spawned agent calls `crtr agent submit`; prints submitted content
allowed-tools: Bash(crtr:*)
---

The user wants to await a spawned agent's submission via `crtr agent await`. Begin by following the CLI guidance below.

!`crtr agent await --help`

$ARGUMENTS
